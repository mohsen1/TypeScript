# Comprehensive Code Review: TypeScript to Rust/WASM Migration

**Generated:** 2026-01-04
**Reviewer:** Gemini 3 Pro (via ask-gemini.mjs)
**Scope:** All major Rust source files in `wasm/src/`

---

## Executive Summary

This document contains a comprehensive code review of the TypeScript-to-Rust migration, generated by running the Gemini 3 Pro AI model against each major source file in the `wasm/src/` directory. The review identifies correctness issues, performance regressions, and deviations from idiomatic Rust patterns.

### Issue Counts by Severity

| Severity | Count | Description |
|----------|-------|-------------|
| **BLOCKER** | 10 | Correctness bugs, undefined behavior, memory safety. Cannot merge. |
| **CRITICAL** | 12 | Performance regressions, API misuse, missing error handling. Should not merge. |
| **MAJOR** | 9 | Non-idiomatic code, maintainability issues, missing tests. Fix before merge. |
| **MINOR** | 6 | Style, naming, documentation. Note for follow-up. |

### Files Reviewed

1. `scanner.rs` - Token definitions and keyword mapping
2. `scanner_impl.rs` - Scanner implementation
3. `parser_impl.rs` - Parser implementation
4. `binder.rs` - Symbol binding and scoping
5. `emitter.rs` - Code generation
6. `checker/mod.rs` - Type checker module organization
7. `checker/relations.rs` - Type relationship checking
8. `checker/narrowing.rs` - Type narrowing and control flow
9. `interner.rs` - String interning
10. `lib.rs` - Crate root and WASM bindings

---

## scanner.rs

**Verdict:** 0 blockers, 0 critical, 1 major, 1 minor. **CHANGES REQUESTED**

### Issues

#### [MAJOR] Line 315 - Hardcoded constant in safety check

**Problem:** The `try_from_u16` function uses a hardcoded value `166` for the bounds check, which duplicates the source of truth defined in `LAST_TOKEN`. If the enum is expanded, this check will become incorrect, potentially leading to Undefined Behavior in the `unsafe` block.

**Evidence:**
```rust
pub fn try_from_u16(value: u16) -> Option<SyntaxKind> {
    // Valid token range is 0-166 (Unknown to DeferKeyword)
    if value <= 166 {
        // SAFETY: We've verified the value is in the valid enum range.
        Some(unsafe { std::mem::transmute(value) })
    } else {
        None
    }
}
```

**Fix:** Use the `LAST_TOKEN` constant:
```rust
if value <= Self::LAST_TOKEN as u16 {
```

#### [MINOR] Line 608 - Missing `Display` implementation

**Problem:** `SyntaxKind` lacks a `Display` implementation, forcing consumers to re-implement string mapping.

**Fix:** Implement `std::fmt::Display` for `SyntaxKind` using existing static mapping functions.

---

## scanner_impl.rs

**Verdict:** 3 blockers, 2 critical, 1 major. **CHANGES REQUESTED**

### Issues

#### [BLOCKER] Line 1367 - UTF-8 decoding panic risk

**Problem:** The scanner manually increments `self.pos += 1` in loops that process potentially multi-byte characters. `char_code_unchecked` uses `self.source[index..]` which panics if `index` is not on a valid UTF-8 character boundary. If the source contains any non-ASCII character, the scanner will panic.

**Evidence:**
```rust
} else {
    if let Some(c) = char::from_u32(ch) {
        result.push(c);
    }
    self.pos += 1; // <--- ERROR: Advances 1 byte even if ch is multi-byte
}
```

**Fix:** Always advance by the character length using `char_len_at`:
```rust
let len = self.char_len_at(self.pos);
self.pos += len;
```

#### [CRITICAL] Line 309 - Stale `token_value`

**Problem:** `scan()` does not reset `token_value`. If `scan()` encounters a token that doesn't set a value (like punctuation), `token_value` retains the string from the previous token.

**Fix:** Clear the token value at the start of `scan()`:
```rust
self.token_value.clear();
```

#### [CRITICAL] Line 1200 - Excessive allocation in identifier scanning

**Problem:** `scan_identifier` allocates a new `String` for every identifier to check if it's a keyword, creating massive allocator pressure.

**Evidence:**
```rust
let text = self.substring(start, self.pos); // Allocates String
self.token_value = text.clone(); // Allocates again
```

**Fix:** Perform keyword lookup on the slice before allocating:
```rust
let text_slice = &self.source[start..self.pos];
self.token = crate::scanner::text_to_keyword(text_slice).unwrap_or(SyntaxKind::Identifier);
self.token_value = text_slice.to_string();
```

#### [MAJOR] Line 1633 - Incorrect Identifier Start/Part logic

**Problem:** The scanner treats any character `> 127` as a valid identifier start/part, which is incorrect according to ECMAScript spec (which allows specific Unicode categories).

**Fix:** Use the `unicode-xid` crate for proper `ID_Start` and `ID_Continue` properties.

#### [MAJOR] Line 1340 - Incorrect escape sequence handling

**Problem:** `scan_string` doesn't handle hex/unicode escapes (`\xHH`, `\uHHHH`) inside standard strings.

**Fix:** Implement full escape sequence parsing similar to `scan_template_escape_sequence`.

#### [MINOR] Line 303 - Token start position logic

**Problem:** Verify `get_token_start()` behavior against TypeScript's `tokenPos` semantics.

---

## parser_impl.rs

**Verdict:** 2 blockers, 2 critical, 2 major. **CHANGES REQUESTED**

### Issues

#### [BLOCKER] Line 1882 - Missing BigIntLiteral in type parsing

**Problem:** The parser fails to recognize BigInt literals in type positions (e.g., `type T = 100n;`).

**Fix:** Add BigIntLiteral case to `parse_primary_type`.

#### [CRITICAL] Line 1418 - Import specifiers reject keywords

**Problem:** `import { type } from 'mod'` is valid TypeScript but `parse_import_specifier` calls `parse_identifier()` which strictly rejects keywords.

**Fix:** Use `parse_binding_identifier()` which accepts contextual keywords.

#### [CRITICAL] Line 1523 - Export specifiers reject keywords

**Problem:** Same issue as above for `export { type }`.

**Fix:** Use `parse_binding_identifier()`.

#### [MAJOR] Line 885 - Invalid shorthand computed property binding

**Problem:** The parser allows `const { [x] } = y` which is invalid. Computed property names in binding patterns must have a renaming colon.

**Fix:** Check if `property_name` is a computed property name when no colon is present and report an error.

#### [MAJOR] Line 727 - Invalid await in regular for-loop

**Problem:** `await` modifier is parsed for all for-loops but `for await (;;)` is a syntax error.

**Fix:** Report error if `await_modifier` is true but statement is not `ForOfStatement`.

#### [MINOR] Line 2198 - Missing parse_bigint_literal helper

**Problem:** Ensure `parse_bigint_literal` is implemented.

---

## binder.rs

**Verdict:** 0 blockers, 2 critical, 2 major, 1 minor. **CHANGES REQUESTED**

### Issues

#### [CRITICAL] Line 370 - Missing `var` hoisting processing

**Problem:** The binder collects `var` declarations into `hoisted_vars` but never processes them, meaning `var` declarations are not declared in function scope.

**Fix:** Implement and call `process_hoisted_vars()`.

#### [CRITICAL] Line 408 - `collect_hoisted_declarations` ignores non-block statements

**Problem:** The hoisting logic only recurses into `Block` nodes, failing to find `var` declarations in single-statement bodies (e.g., `if (true) var x = 1;`).

**Fix:** Handle statement nodes recursively, not just blocks.

#### [MAJOR] Line 103 - Missing `members` and `exports` in Symbol struct

**Problem:** The `Symbol` struct lacks `members` and `exports` fields, so class member symbols are lost when popping scope.

**Fix:** Add `members` and `exports` fields to `Symbol` and populate them in `bind_class_declaration`.

#### [MAJOR] Line 885 - Incorrect Control Flow Analysis for Switch Fallthrough

**Problem:** The implementation resets `current_flow` to the switch condition for every case clause, breaking control flow analysis for fallthrough.

**Fix:** Maintain a `fallthrough_flow` to preserve flow from previous case.

#### [MINOR] Line 411 - Redundant `.is_none()` check

**Problem:** Redundant check before `if let Some(...)`.

---

## emitter.rs

**Verdict:** 1 blocker, 4 critical, 1 major, 1 minor. **CHANGES REQUESTED**

The emitter implementation is **severely incomplete**. It systematically ignores critical AST fields (generics, modifiers, heritage clauses, return types), producing lossy output.

### Issues

#### [BLOCKER] Line 435 - Invalid string literal emission

**Problem:** The emitter wraps string content in quotes without escaping, producing invalid syntax for strings containing quotes or newlines.

**Fix:** Implement proper string escaping.

#### [CRITICAL] Lines 895, 915, 936 - Dropped Declaration Modifiers

**Problem:** The emitter ignores `modifiers` on declarations (`export`, `declare`, `private`, `static`, `abstract`, `async`).

**Fix:** Implement `emit_modifiers` helper.

#### [CRITICAL] Lines 905, 922, 1024, 1073 - Dropped Generics

**Problem:** The emitter ignores `type_parameters` and `type_arguments`. `function foo<T>()` becomes `function foo()`.

**Fix:** Emit type parameters before parameter list.

#### [CRITICAL] Lines 925, 1026 - Dropped Heritage Clauses

**Problem:** Inheritance is completely stripped. `class Foo extends Bar` becomes `class Foo`.

**Fix:** Implement emission for `heritage_clauses`.

#### [CRITICAL] Line 910 - Dropped Return Types

**Problem:** Function return types are ignored.

**Fix:** Check and emit `type_annotation` before the body.

#### [MAJOR] Multiple lines - Untracked TODOs

**Problem:** The file has many `TODO` comments indicating missing features without issue tracking.

**Fix:** Implement missing features or link TODOs to GitHub issues.

#### [MINOR] Line 1086 - Dropped Import Attributes

**Problem:** Import attributes (e.g., `assert { type: "json" }`) are ignored.

---

## checker/mod.rs

**Verdict:** 0 blockers, 0 critical, 0 major, 1 minor. **APPROVED**

The module definition is clean and correctly organizes submodules.

### Issues

#### [MINOR] Line 27 - Inconsistent type re-exports

**Problem:** Several public types are missing from re-export list (`ThisTypeMarker`, `UniqueSymbolType`, `MappedTypeModifier`, `element_flags`).

**Fix:** Add missing types to the `pub use` list.

---

## checker/relations.rs

**Verdict:** 0 blockers, 3 critical, 1 major, 1 minor. **CHANGES REQUESTED**

### Issues

#### [CRITICAL] Line 306 - Missing implementation of optional property checking

**Problem:** Structural compatibility check fails immediately if a property is missing in source, ignoring optional properties.

**Fix:** Check if target symbol has the `OPTIONAL` flag before returning false.

#### [CRITICAL] Lines 389-393 - Empty logic block for signature parameter count check

**Problem:** The function contains an empty `if` block for parameter count checking, effectively disabling arity validation.

**Fix:** Implement the check properly:
```rust
if source.min_argument_count > target.parameters.len() as u32 {
    return false;
}
```

#### [CRITICAL] Line 526 - Incorrect Enum compatibility check

**Problem:** Enums are compared by string name equality, which is incorrect. Different enum declarations with the same name should not be compatible.

**Fix:** Remove name comparison; enums are nominally typed.

#### [MAJOR] Lines 103, 115, 127, 139 - Unnecessary vector cloning

**Problem:** The code clones union/intersection type vectors just to iterate, causing unnecessary allocations in hot paths.

**Fix:** Iterate over references directly:
```rust
for &member in &u.types {
```

#### [MINOR] Lines 405-412 - Incomplete parameter type checking logic

**Problem:** Logic doesn't fully account for excess parameters in function assignability.

---

## checker/narrowing.rs

**Verdict:** 3 blockers, 1 critical, 1 major. **CHANGES REQUESTED**

There are significant Rust ownership and borrow checker violations in this file.

### Issues

#### [BLOCKER] Line 34 - Borrow checker violation in narrow_type_by_typeof

**Problem:** The code holds an immutable borrow of `self.types` while trying to call mutable methods. This pattern repeats throughout the file.

**Evidence:**
```rust
let Some(typ) = self.types.get(type_id) else { ... };
if let Type::Union(u) = typ {
    return self.types.create_union_type(matching_types); // ERROR
}
```

**Fix:** Check for union and clone the types vector first, dropping the borrow.

#### [BLOCKER] Line 887 - Borrow checker violation in get_type_guard_from_expression

**Problem:** Code holds a reference to `node` while calling methods requiring `&mut self`.

**Fix:** Clone the node or extract fields before calling helpers.

#### [BLOCKER] Line 423 - Recursive mutable borrow in get_apparent_type_worker

**Problem:** Recursively calling `&mut self` methods while holding references.

**Fix:** Extract constraint ID, drop borrow, then recurse.

#### [CRITICAL] Line 25 - Incorrect typeof narrowing logic

**Problem:** Using `type_flags::OBJECT` to identify functions is incorrect; regular objects also have this flag.

**Fix:** Inspect the `Type` variant or check for signatures instead of just flags.

#### [MAJOR] Line 274 - Infinite recursion risk in get_awaited_type_with_depth

**Problem:** Recursion limit returns the wrapper type, which may confuse callers.

**Fix:** Return `any_type` or `unknown_type` when recursion limit is hit.

---

## interner.rs

**Verdict:** 0 blockers, 1 critical, 1 major, 1 minor. **CHANGES REQUESTED**

### Issues

#### [CRITICAL] Line 50 - Inefficient memory usage (double allocation)

**Problem:** The `Interner` stores full `String` copies in both the map and the vector, doubling heap memory usage.

**Fix:** Use `Rc<str>` to share the underlying string allocation:
```rust
pub struct Interner {
    map: FxHashMap<Rc<str>, Atom>,
    strings: Vec<Rc<str>>,
}
```

#### [MAJOR] Line 18 - API misuse risk (public field)

**Problem:** `Atom(pub u32)` exposes internal index, allowing construction of invalid atoms.

**Fix:** Make the field private and provide an accessor.

#### [MINOR] Line 1 - Missing `Deserialize`

**Problem:** `Atom` derives `Serialize` but not `Deserialize`.

---

## lib.rs

**Verdict:** 2 blockers, 3 critical, 2 major. **CHANGES REQUESTED**

This review also covers cross-cutting concerns visible from the crate root.

### Issues

#### [BLOCKER] scanner_impl.rs - Panic on multibyte characters

**Problem:** Scanner byte-index iteration panics on non-ASCII input.

**Fix:** Use `char_len_at` to advance position.

#### [BLOCKER] scanner_impl.rs:1367 - Panic in template escape scanning

**Problem:** `scan_template_escape_sequence` slices the source without verifying character boundaries.

**Fix:** Verify hex digits before slicing.

#### [CRITICAL] scanner.rs:248 - Unsafe transmute relying on fragile invariants

**Problem:** `try_from_u16` uses unsafe transmute that will cause UB if enum discriminants have holes.

**Fix:** Use `num_enum` crate or generate safe `TryFrom` with a match statement.

#### [CRITICAL] lib.rs:133 - Allocation in hot-path string comparison

**Problem:** `compare_strings_case_insensitive` allocates two new `String`s for every comparison.

**Fix:** Use iterator-based comparison to avoid allocation.

#### [CRITICAL] scanner_impl.rs:130 - Excessive cloning in lookahead

**Problem:** `save_state` clones `token_value` String for every lookahead operation.

**Fix:** Use `Rc<String>` or represent token value as a slice range.

#### [MAJOR] parser_impl.rs:20 - Recursion depth risk

**Problem:** Deeply nested ASTs will cause stack overflow.

**Fix:** Implement recursion limit check or refactor to iterative parsing.

#### [MAJOR] checker/types/type_def.rs - Missing Type size optimization

**Problem:** `ObjectType` contains HashMaps and Vecs, making it expensive behind `Box`.

**Fix:** Arena-allocate `ObjectType` separately and use indices in the `Type` enum.

---

## Recommendations

### Immediate Priority (Blockers)

1. **Fix UTF-8 handling in scanner** - The scanner will panic on any non-ASCII source code
2. **Fix borrow checker violations in narrowing.rs** - This code cannot compile correctly
3. **Fix string literal escaping in emitter** - Produces invalid output

### High Priority (Critical)

1. **Clear token_value between scans** - Causes incorrect token values
2. **Fix optional property checking** - Basic TypeScript assignability broken
3. **Fix enum comparison** - Nominal typing violated
4. **Reduce allocations in hot paths** - Significant performance regression

### Medium Priority (Major)

1. **Complete emitter implementation** - Modifiers, generics, heritage clauses
2. **Implement var hoisting** - Fundamental JavaScript semantics
3. **Add Symbol members/exports** - Required for class/module support
4. **Optimize Interner memory usage** - 2x memory overhead

### Low Priority (Minor)

1. **Re-export consistency in checker/mod.rs**
2. **Display implementation for SyntaxKind**
3. **Add Deserialize to Atom**

---

## Conclusion

The TypeScript-to-Rust migration has a solid architectural foundation with arena-based memory management. However, there are **critical stability issues** in the scanner (UTF-8 handling) and type checker (borrow violations) that must be addressed before the code can be considered production-ready. The emitter is currently a prototype that produces partial/broken output.

**Total: 10 blockers, 12 critical, 9 major, 6 minor issues requiring attention.**
