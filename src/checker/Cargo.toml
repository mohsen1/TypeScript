[package]
name = "ts-checker"
version = "0.1.0"
edition = "2021"
description = "TypeScript class type checker implementation in Rust"

[lib]
path = "lib.rs"

[dependencies]

[dev-dependencies]
